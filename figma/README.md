# Figma — design assets

This team’s primary UI is implemented directly in Flutter (theme in `flutter_app/lib/main.dart`, purple accent `#B19CD9`).

There is **no separate Figma file** bundled in-repo. Replace this readme with screenshots or exported links if your group adds FIGMA URLs:

```
https://www.figma.com/file/YOUR_TEAM_FILE_ID/GuidePoint
```

Screenshots acceptable for graders: QR scanner → voice hub → navigation (camera preview + waypoint text).
