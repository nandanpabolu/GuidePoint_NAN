# Integration — Pedometer fallback (`pedometer`)

Feeds **`StepCountService`** → fused crude distance toward next node when advanced vision cues unavailable.

**Platform nuance:**

- Accuracy better on hardware vs emulator.  
- Activity recognition prompt on Android 10+ manifests.
